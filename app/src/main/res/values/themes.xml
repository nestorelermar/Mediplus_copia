<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mediplus" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/azul</item>
        <item name="colorSecondary">@color/azul</item><!--@color/rojo-->
        <item name="android:statusBarColor">@color/blanco_app</item>
    </style>

    <style name="Theme.Mediplus" parent="Base.Theme.Mediplus" />
    <style name="Theme.Mediplus.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.Mediplus.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Mediplus.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>